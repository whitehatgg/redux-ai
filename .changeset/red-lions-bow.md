```markdown
---
'@redux-ai/react': patch
---

fix(ci): update GitHub Actions workflow and test configuration

- Update pnpm version to 8.15.9 in CI workflow
- Remove frozen lockfile requirement for CI builds
- Fix test configuration paths and patterns
- Ensure proper handling of workspace packages in CI
```
