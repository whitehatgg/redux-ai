#!/usr/bin/env sh

# Run TypeScript type checking
pnpm exec tsc --noEmit || exit 1

# Run format checks for all packages using prettier directly
pnpm exec prettier --check "**/*.{ts,tsx,js,jsx,json,md,css}" --ignore-path .prettierignore || exit 1

# Run lint-staged
pnpm lint-staged || exit 1