name: Pull Request

permissions:
  pull-requests: write
  contents: write

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  my_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run a command
        run: echo "Hello, world!"